language: python

python:
 - 3.6

sudo: false

install:
 - pip install pyflakes pycodestyle
 - pip install -r requirements.txt

script:
 # Static analysis
 - pyflakes .
 - pycodestyle --statistics --count .

matrix:
  fast_finish: true
